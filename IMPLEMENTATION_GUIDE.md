# מדריך יישום - הוראות מדויקות למודל מבצע (Portfolio Analysis Web App)

**מטרת המסמך:** הוראות עבודה מדויקות, קובץ-אחר-קובץ, למימוש כל פריט מ-**סעיף 13 (ROADMAP)** בקובץ ה-HANDOVER. המסמך הזה **אינו** מכיל קוד - הוא מכיל תיאור מדויק של מה לעשות, איפה, ולפי אילו כללים קיימים בפרויקט, כך שמודל חלש/זול יותר (או מפתח) יוכל לכתוב את הקוד בעצמו בלי לאבד הקשר.

**חשוב לקרוא לפני כל פריט:**
1. תמיד לקרוא קודם את `HANDOVER.md` המלא (במיוחד סעיפים 1-3, 6) כדי להבין את הארכיטקטורה והעקרונות הקיימים.
2. **עיקרון-על שאסור לשבור בשום פריט:** ה-LLM (Gemini/CrewAI) **לעולם לא מחשב מספרים** - כל חישוב נשאר ב-Python. אם פריט מסוים "נוגע" ב-AI, לוודא שהעיקרון הזה נשמר.
3. **עיקרון-על שני:** כל endpoint חדש שניגש ל-`portfolios`/`holdings`/`analysis_runs` **חייב** לסנן לפי `user_id` (למשל דרך `_get_owned_portfolio` הקיים) - ה-backend עוקף RLS לגמרי (`service_role key`), כך שזו ההגנה היחידה בפועל.
4. **לאחר כל שינוי:** להריץ `python -m py_compile <קובץ>` או `python -c "import ast; ast.parse(open('<קובץ>').read())"` על כל קובץ Python שהשתנה, לוודא syntax תקין לפני מסירה. עבור React - להריץ `npm run build` בתיקיית `frontend/`.
5. **לא לגעת** בתיקיית `portfolio-agent/` (הפרויקט הישן) אלא אם מפורש אחרת - כל הפיתוח הפעיל הוא ב-`portfolio-app/`.

---

## 13.1 תשתית / Deployment

### פריט 1 - Deployment בפועל (Render + Vercel)

**מטרה:** להעלות את ה-backend ל-Render ואת ה-frontend ל-Vercel, כך שהאפליקציה תהיה נגישה בכתובת אינטרנט אמיתית, לא רק `localhost`.

**הוראות:**
1. **Backend (Render):** ליצור "Web Service" חדש ב-Render, לחבר את ה-repo, root directory = `portfolio-app/backend`. Build command: `pip install -r requirements.txt`. Start command: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`.
2. להעתיק את כל המשתנים מ-`.env.example` ולהגדיר אותם בפועל בלשונית "Environment" ב-Render (לא להעלות `.env` בפועל ל-repo).
3. **Frontend (Vercel):** לייבא את ה-repo, root directory = `portfolio-app/frontend`. Framework preset: Vite. Build command: `npm run build`. Output directory: `dist`.
4. להגדיר ב-Vercel את משתני הסביבה `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, ואת `VITE_API_BASE` להצביע לכתובת ה-backend האמיתית מ-Render (לא `localhost`).
5. אחרי הפריסה - לעדכן את `CORS` ב-`main.py` (ראו פריט 7 בהמשך) לדומיין האמיתי של Vercel, **לפני** שמכריזים על סיום.
6. **בדיקת קבלה:** לפתוח את כתובת ה-Vercel בדפדפן, לבצע signup חדש, ליצור תיק, להעלות קובץ ולהריץ ניתוח מקצה-לקצה - הכל דרך האינטרנט, לא מקומית.

### פריט 2 - CI/CD בסיסי (GitHub Actions)

**מטרה:** הרצה אוטומטית של בדיקות syntax/build בכל push, לפני שקוד שבור מגיע ל-main.

**הוראות:**
1. ליצור קובץ workflow חדש בנתיב `.github/workflows/ci.yml` בשורש ה-repo.
2. ה-workflow צריך להריץ שני jobs נפרדים: אחד ל-backend (Python) ואחד ל-frontend (Node).
3. **Job Backend:** להתקין Python 3.12, להריץ `pip install -r portfolio-app/backend/requirements.txt`, ואז להריץ בדיקת syntax על כל קובצי ה-`.py` תחת `portfolio-app/backend/app/` (`python -m py_compile` על כל קובץ, או `python -m compileall`). אם קיימים בדיקות `pytest` - להריץ גם אותן.
4. **Job Frontend:** להתקין Node 20, להריץ `npm install` ואז `npm run build` בתיקיית `portfolio-app/frontend`.
5. ה-workflow צריך לרוץ על `push` ו-`pull_request` לענף `main`.
6. **בדיקת קבלה:** לבצע push קטן (למשל שינוי רווח לבן) ולוודא שה-workflow רץ ומצליח ב-GitHub Actions tab.

### פריט 3 - Containerization (Docker)

**מטרה:** קובץ `Dockerfile` ל-backend, כדי שסביבת הריצה תהיה זהה בכל מקום.

**הוראות:**
1. ליצור `Dockerfile` בתיקיית `portfolio-app/backend/` (לא לדרוס קבצים קיימים).
2. Base image: `python:3.12-slim`. להעתיק את `requirements.txt` ולהתקין תלויות **לפני** העתקת שאר הקוד (לניצול Docker layer cache).
3. להעתיק את שאר קוד ה-backend, לחשוף פורט 8000, ולהריץ `uvicorn app.main:app --host 0.0.0.0 --port 8000` כ-CMD.
4. **חשוב:** לשים לב לקבצי הפונט העברי (Arial/Tahoma, מוזכרים בסעיף 2ב של HANDOVER תחת `pdf_report.py`) - אם הפרויקט תלוי בפונטים ספציפיים ל-Windows, יש לוודא זמינות מקבילה ב-Linux container (למשל DejaVu Sans או להטמיע קובץ פונט בפועל בתוך ה-image), אחרת PDF בעברית עלול להישבר בסביבת Docker.
5. **בדיקת קבלה:** `docker build` מצליח, `docker run` מעלה את השרת, ו-`GET /health` מחזיר תשובה תקינה מתוך הקונטיינר.

### פריט 4 - Rate-limiting/Cooldown משותף (Redis)

**מטרה:** להחליף את ה-state בזיכרון-תהליך (הקיים כיום ב-`routers/analysis.py` וב-`routers/recommendations.py`) במנגנון משותף שעובד גם עם כמה instances במקביל.

**הוראות:**
1. **לפני הכל:** לוודא שפריט 1 (Deployment) כבר קיים ורלוונטי בפועל - אם רצים על instance יחיד תמיד, זה פריט בעדיפות נמוכה.
2. לבחור Redis hosting חינמי (למשל Upstash - יש תוכנית חינמית) והוסיף משתנה סביבה `REDIS_URL`.
3. ליצור מודול חדש `backend/app/rate_limit.py` שמרכז את כל לוגיקת ה-rate limiting (כרגע מפוזרת/משוכפלת בין `analysis.py` ל-`recommendations.py`) - שתי פונקציות: `check_and_record(user_id: str, key: str, max_per_hour: int, min_seconds_between: int) -> None` שזורקת HTTPException(429) אם חריגה, אחרת רושמת timestamp חדש ב-Redis (למשל עם `INCR`+`EXPIRE`, או sorted set עם timestamps).
4. להחליף את הקוד הקיים ב-`analysis.py` וב-`recommendations.py` בקריאה לפונקציה המשותפת הזו, **בלי לשנות את ההתנהגות החיצונית** (עדיין 5/שעה + 20 שניות מינימום, אלא אם מוחלט אחרת).
5. **בדיקת קבלה:** להריץ שני תהליכי uvicorn מקומיים על פורטים שונים (מדמה 2 instances), לוודא שה-cooldown של משתמש אחד "נראה" משני התהליכים גם יחד (לא state נפרד לכל אחד).

### פריט 5 - הרצת `test_flow.ps1`

**מטרה:** לוודא שהסקריפט הקיים (`backend/test_flow.ps1`) באמת עובד מקצה-לקצה מול Supabase אמיתי.

**הוראות:**
1. לקרוא את הסקריפט הקיים במלואו קודם - **לא לשכתב אותו** אלא רק לתקן שגיאות אם נתקלים בהן בפועל בזמן הרצה.
2. להריץ אותו על סביבת פיתוח (לא production) עם `.env` תקין, ולתעד כל שגיאה שמתקבלת.
3. אם מתקבלת שגיאה - לתקן רק את הבעיה הספציפית שנתקלים בה (לרוב URL/טוקן שגויים או endpoint ששונה מאז שנכתב הסקריפט), לא לשנות את מבנה הבדיקה.
4. **בדיקת קבלה:** הסקריפט רץ מתחילתו ועד סופו (signup→login→create portfolio→add holdings→run analysis→get PDF signed URL→history) ללא שגיאה, ומדפיס סטטוס הצלחה בכל שלב.

---

## 13.2 אבטחה

### פריט 6 - Timeout + caching לאימות מול Supabase

**מטרה:** להוסיף timeout מפורש לקריאת הרשת ב-`auth.py` (`supabase.auth.get_user(token)`), שכרגע (ראו סעיף 6 פריט 12 ב-HANDOVER) עושה קריאת API אמיתית בכל בקשה, עם מטמון 60 שניות שכבר קיים.

**הוראות:**
1. לפתוח את `backend/app/db.py` (הקובץ שיוצר את ה-Supabase client) ולבדוק אם ה-client library תומך בהעברת פרמטר timeout ל-`httpx` הפנימי (ספריית `supabase-py` בדרך כלל בונה `httpx.Client` מתחתיו).
2. אם קיימת אפשרות להעביר `timeout=` בבניית ה-client - להגדיר ערך סביר (למשל 5-10 שניות).
3. ב-`auth.py`, לעטוף את הקריאה ל-`supabase.auth.get_user(token)` ב-`try/except` שתופס גם שגיאות timeout/network (לא רק שגיאות auth), ומחזיר **503** (Service Unavailable) במקרה כזה - להבדיל במפורש מ-**401** (טוקן לא תקין) - ההבחנה הזו כבר קיימת חלקית (ראו סעיף 8 פריט 4 ב-HANDOVER), רק לוודא שגם כשל-timeout ספציפי נתפס תחת אותה הבחנה.
4. **בדיקת קבלה:** לדמות timeout (למשל ע"י חסימה זמנית של גישת רשת ל-Supabase בסביבת בדיקה) ולוודא שהתשובה למשתמש היא 503 עם הודעה ברורה, לא 500 גולמי.

### פריט 7 - CORS מוגבל לדומיין production

**מטרה:** להחליף את `allow_origins=["*"]` ב-`main.py` בדומיין הספציפי של ה-frontend בפרודקשן.

**הוראות:**
1. **תלוי בפריט 1** (Deployment) - יש קודם לדעת מה כתובת ה-Vercel הסופית.
2. ב-`main.py`, לשנות את הגדרת ה-`CORSMiddleware` כך ש-`allow_origins` יהיה רשימה מפורשת: הדומיין של Vercel (production) + `http://localhost:5173` (או הפורט המקומי בפועל של Vite) לפיתוח מקומי.
3. עדיף לקרוא את רשימת הדומיינים המותרים ממשתנה סביבה (`ALLOWED_ORIGINS`, מחרוזת מופרדת בפסיקים) כדי לא לצטרך לשנות קוד בכל שינוי דומיין.
4. **בדיקת קבלה:** בקשת fetch מדומיין לא-מורשה (למשל דרך `curl -H "Origin: https://evil.example.com"`) נחסמת ע"י הדפדפן/מוחזרת ללא header `Access-Control-Allow-Origin` מתאים; בקשה מהדומיין האמיתי עובדת כרגיל.

### פריט 8 - סקירת RLS/הרשאות לפני production

**מטרה:** לוודא שכל endpoint (כולל אלו שנוספו לאחרונה - `recommendations.py`, סעיף 12 ב-HANDOVER) מסנן לפי `user_id`.

**הוראות:**
1. לעבור על **כל** הפונקציות בכל קובץ תחת `backend/app/routers/` ולוודא שכל שאילתה שקוראת/כותבת ל-`portfolios`, `holdings`, או `analysis_runs` עוברת דרך `_get_owned_portfolio(supabase, portfolio_id, user_id)` (או פונקציה מקבילה) - **לא** שאילתה ישירה ללא סינון.
2. תשומת לב מיוחדת ל-`routers/recommendations.py` (חדש, סעיף 12): לוודא שכאשר `portfolio_id` מסופק, יש בדיקה מפורשת שהתיק שייך למשתמש המחובר לפני שליפת האחזקות שלו - **לא** לסמוך על כך שהמשתמש "בטח" שולח רק את התיק שלו.
3. לכתוב רשימת בדיקה (checklist) פשוטה - טבלה עם כל endpoint ועמודה "מסונן לפי user_id? כן/לא" - ולסמן ידנית.
4. **בדיקת קבלה:** יצירת שני משתמשים (A ו-B) בבדיקה ידנית, וניסיון של משתמש B לגשת ל-`portfolio_id` ששייך למשתמש A דרך כל endpoint ברשימה - צריך לקבל 403/404 בכל מקרה, לא נתונים.

### פריט 9 - Secrets management

**מטרה:** לוודא שאין מפתחות רגישים בקוד/repo, ושכולם מוזרקים כ-environment variables בפלטפורמת ה-hosting.

**הוראות:**
1. להריץ חיפוש (`grep`) בתוך כל ה-repo אחרי דפוסים חשודים: `GOOGLE_API_KEY=`, `SUPABASE_SERVICE`, `eyJ` (תחילת JWT טיפוסי) - לוודא שאין מפתח אמיתי מוטבע בקוד או ב-`.env` שהוכנס בטעות ל-git.
2. לוודא שקובץ `.gitignore` בשורש הפרויקט (או בתיקיות `backend/`/`frontend/`) כולל `.env` באופן מפורש.
3. אם נמצא מפתח שהודלף בעבר (ראו גם סעיף 6 פריט 15 ב-HANDOVER, על JWT שהודבק בצ'אט) - **לסובב (rotate)** את המפתח בפועל דרך קונסולת Supabase/Google Cloud, לא רק להסיר אותו מהקוד.
4. **בדיקת קבלה:** `git log -p | grep -i "api_key"` (או כלי ייעודי כמו `git-secrets`/`trufflehog`) לא מוצא סודות בהיסטוריית ה-commits.

---

## 13.3 תפעול / Observability

### פריט 10 - Error tracking (Sentry)

**מטרה:** ערוץ ריכוזי לשגיאות בפרודקשן, גם ב-backend וגם ב-frontend.

**הוראות:**
1. ליצור חשבון Sentry חינמי, וליצור שני "פרויקטים" נפרדים בתוכו: אחד ל-Python (backend), אחד ל-React (frontend).
2. **Backend:** להתקין `sentry-sdk` (עם extra `[fastapi]`), ולאתחל אותו ב-`main.py` **לפני** יצירת ה-app (`sentry_sdk.init(dsn=..., traces_sample_rate=0.1)`), כך שה-exception handler הגלובלי הקיים (סעיף 8 פריט 5 ב-HANDOVER) ימשיך לעבוד כרגיל, אבל השגיאות גם יישלחו ל-Sentry.
3. **Frontend:** להתקין `@sentry/react`, ולאתחל ב-`main.jsx`. ב-`ErrorBoundary.jsx` (קיים, סעיף 2ב), להוסיף קריאה ל-`Sentry.captureException(error)` בתוך `componentDidCatch`, **בנוסף** ל-`console.error` הקיים (לא להחליף).
4. את ה-DSN (מפתח פרטי-אך-לא-סודי) להעביר כמשתנה סביבה (`SENTRY_DSN` ל-backend, `VITE_SENTRY_DSN` ל-frontend).
5. **בדיקת קבלה:** לגרום בכוונה לשגיאה (למשל endpoint לא קיים) ולוודא שהיא מופיעה בדשבורד Sentry תוך דקה.

### פריט 11 - Dashboard/alert על מכסות Gemini/yfinance

**מטרה:** להתריע **לפני** שהמכסה נגמרת, לא רק כשמשתמשים כבר נתקלים בכשל.

**הוראות:**
1. לבדוק תחילה אם Google AI Studio/Gemini מציעים דשבורד מובנה למעקב מכסה יומית (סביר שכן) - אם כן, זו נקודת ההתחלה, לא לבנות מערכת חדשה.
2. ב-`engine/ai_analysis.py` וב-`engine/cash_allocation.py`, להוסיף `logger.warning(...)` בכל פעם שמתבצע fallback מ-Gemini הראשי (`gemini-3.1-flash-lite`) למשני (`gemini-3.5-flash`) - זהו האינדיקטור המוקדם ביותר שקיים כבר בקוד לבעיית מכסה, ופשוט צריך לוודא שהוא **נרשם ונראה** (למשל דרך Sentry, פריט 10, כ-warning event).
3. אופציונלי בלבד, בעדיפות נמוכה יותר: מונה גלובלי פשוט (in-memory, כמו ב-rate limiting הקיים) שסופר קריאות ל-Gemini ביום, ומדפיס אזהרה ללוג כשעובר סף (למשל 80% ממכסה ידועה).
4. **בדיקת קבלה:** לוג ה-warning מופיע בפועל בבדיקה מקומית כשמדמים כשל של המודל הראשי (למשל ע"י ניתוק זמני מהאינטרנט או שינוי זמני לשם מודל שגוי).

### פריט 12 - היסטוריה להמלצות "פיזור מזומן"

**מטרה:** לשמור לוג של המלצות פיזור מזומן שהופקו, בדומה לטבלת `analysis_runs` הקיימת.

**הוראות:**
1. **DB (`database/schema.sql`):** להוסיף טבלה חדשה `cash_allocation_runs` עם עמודות דומות בעיקרון ל-`analysis_runs` (ראו סעיף 2ב ב-HANDOVER): `id`, `user_id`, `portfolio_id` (nullable - כי זה אופציונלי, ראו סעיף 12.2), `cash_amount`, `recommendation_json` (jsonb - האחוזים/סכומים שהתקבלו), `created_at`. להוסיף RLS policy זהה בעקרון לזו של `analysis_runs`.
2. **Backend (`routers/recommendations.py`):** אחרי חישוב ההמלצה בהצלחה, להוסיף `INSERT` לטבלה החדשה (בדומה לאיך ש-`routers/analysis.py` שומר ל-`analysis_runs` - להעתיק את הדפוס, לא להמציא חדש).
3. להוסיף endpoint חדש `GET /recommendations/cash-allocation/history` שמחזיר את ההיסטוריה של המשתמש המחובר בלבד (עם סינון `user_id`, ראו עיקרון-העל למעלה).
4. **Frontend (`CashRecommendation.jsx`):** להוסיף קטע "היסטוריה" בתחתית העמוד, בדומה לאיך ש-`PortfolioDetail.jsx` מציג היסטוריית ניתוחים (ראו סעיף 2ב).
5. **בדיקת קבלה:** להריץ המלצת פיזור מזומן פעמיים ברצף (עם cooldown מתאים), ולוודא ששתי הרשומות מופיעות בהיסטוריה בסדר כרונולוגי נכון.

---

## 13.4 איכות נתונים

### פריט 13 - הגבלת גודל/שורות

**סטטוס:** ✅ כבר מומש (ראו סעיף 8 פריט 2 ב-HANDOVER) - **אין פעולה נדרשת**, אין צורך לכתוב קוד עבור פריט זה.

### פריט 14 - אימות/דיון על גישת המשוב המוקדם לטיקרים לא-מזוהים

**מטרה:** לא כתיבת קוד חדש - אלא **אימות מודעת עם המשתמש** שהגישה הקיימת (batch call יחיד + כשל שקט, ראו סעיף 9 פריט 8 ב-HANDOVER) היא אכן הרצויה, לפני שממשיכים לבנות עליה.

**הוראות (לא-טכניות):**
1. לתאר למשתמש (בעברית פשוטה, לא טכנית) את שלוש האופציות שהיו קיימות במקור, ואת מה שנבחר בפועל בגישת-הביניים.
2. לשאול במפורש: האם הוא מרוצה מהגישה הקיימת, או שרוצה לשנות אותה?
3. רק לאחר תשובה מפורשת - לפתוח משימת קוד חדשה אם נדרש שינוי.

---

## 13.5 UX / Frontend

### פריט 15 - בדיקת `ButtonGroup.jsx` על מובייל אמיתי

**מטרה:** לוודא touch target וריווח תקינים ב-PWA בפועל.

**הוראות:**
1. **זו לא משימת קוד ראשונית - זו משימת בדיקה.** להריץ את ה-frontend (`npm run dev`), לפתוח בדפדפן מובייל אמיתי (או DevTools עם device emulation) ברוחב ~375px, ולבדוק ויזואלית את `Dashboard.jsx` ו-`CashRecommendation.jsx`.
2. לבדוק שגודל המגע (touch target) של כל כפתור ב-`ButtonGroup.jsx` הוא לפחות 44x44px (סטנדרט נגישות מקובל למובייל), ושאין חפיפה בין כפתורים סמוכים.
3. **רק אם נמצאת בעיה בפועל** - לתקן ב-`ButtonGroup.jsx` את ה-CSS/גדלים (padding/min-height), תוך שימוש בטוקנים הקיימים ב-`styles.css` (לא ערכים חדשים "חד-פעמיים" - ראו סעיף 6 פריט 11 ב-HANDOVER).
4. **בדיקת קבלה:** צילום מסך לפני/אחרי במובייל אמיתי, עם אישור שאין חפיפה ושהכפתורים נוחים ללחיצה באצבע.

### פריט 16 - מצב טעינה עקבי בין מסכים

**מטרה:** קומפוננטת loading משותפת, במקום מימוש נפרד בכל מסך.

**הוראות:**
1. ליצור קומפוננטה חדשה `frontend/src/components/LoadingSpinner.jsx` (שם קובץ מוצע - אפשר גם `LoadingState.jsx`) - קומפוננטה גנרית קטנה, בהשראת הדפוס של `ButtonGroup.jsx` הקיים (קומפוננטה גנרית לשימוש חוזר, סעיף 11.2 ב-HANDOVER).
2. להשתמש בטוקני העיצוב הקיימים ("פנקס פיננסי" - `var(--accent)` וכו', ראו סעיף 6 פריט 11) - **לא** לעצב מחדש מאפס.
3. לעבור על `Dashboard.jsx`, `PortfolioDetail.jsx`, ו-`CashRecommendation.jsx`, ולזהות בכל אחד היכן יש כרגע טיפול ב-loading state (בזמן קריאת API), ולהחליף ב-component המשותף החדש - **בלי לשנות את הלוגיקה עצמה** (מתי מציגים/מסתירים), רק את התצוגה.
4. **בדיקת קבלה:** בכל אחד משלושת המסכים, טעינה נראית ויזואלית זהה (אותו spinner/אנימציה), לא שונה בין מסך למסך.

### פריט 17 - נגישות (a11y) בסיסית

**מטרה:** תיוג ARIA + ניגודיות צבעים, בדגש על קידוד ירוק/אדום למשמעות פיננסית.

**הוראות:**
1. ב-`ButtonGroup.jsx`, לוודא שלכל כפתור יש `role="radio"` (או `aria-pressed` אם זה לא radio group אמיתי), ושקבוצת הכפתורים עטופה ב-`role="radiogroup"` עם `aria-label` מתאים בעברית (למשל "רמת סיכון").
2. לבדוק ניגודיות צבעים (contrast ratio) של `--accent` (#1f7a6c) ו-`--danger` (#b4432e) מול הרקע, בכלי כמו WebAIM Contrast Checker - לוודא עמידה ב-WCAG AA (יחס 4.5:1 לטקסט רגיל).
3. **קריטי לאור אופי האפליקציה (משתמשת בירוק/אדום למשמעות פיננסית, לא רק כקישוט):** לוודא שבכל מקום שבו צבע מסמן משמעות (רווח/הפסד), יש **גם** אינדיקטור נוסף שאינו-צבע (למשל סימן `+`/`-`, או אייקון חץ למעלה/למטה) - כדי שמשתמשים עם עיוורון צבעים לא יפספסו את המשמעות.
4. **בדיקת קבלה:** להריץ את הדפדפן עם סימולטור עיוורון צבעים (קיים ב-Chrome DevTools תחת Rendering > Emulate vision deficiencies) ולוודא שאפשר עדיין להבחין בין רווח להפסד.

### פריט 18 - מסך היסטוריה מאוחד

**מטרה:** מסך "כל הפעילות שלי" שמאחד היסטוריית ניתוחי תיקים והמלצות פיזור מזומן.

**הוראות:**
1. **תלוי בפריט 12** (היסטוריה לפיזור מזומן) - יש צורך שקודם תהיה טבלת `cash_allocation_runs` ו-endpoint היסטוריה תואם.
2. ליצור עמוד חדש `frontend/src/pages/ActivityHistory.jsx` שקורא משני מקורות: `GET /portfolios/{id}/analysis` (קיים) ו-`GET /recommendations/cash-allocation/history` (חדש, פריט 12), ומאחד לרשימה כרונולוגית אחת ממוינת לפי תאריך.
3. להוסיף כפתור/קישור ניווט אליו ב-Navbar הקיים ב-`App.jsx` (סעיף 12.5 ב-HANDOVER), לצד "התיקים שלי" ו-"המלצת פיזור מזומן".
4. **בדיקת קבלה:** ביצוע ניתוח תיק אחד + המלצת פיזור מזומן אחת, ולוודא ששניהם מופיעים במסך המאוחד, ממוינים נכון לפי זמן.

---

## 13.6 מנוע ה-AI / Engine

### פריט 19 - בדיקת קצה-לקצה אמיתית ל"פיזור מזומן"

**מטרה:** לוודא שהפיצ'ר עובד בפועל מול Supabase/Gemini אמיתיים, לא רק syntax check.

**הוראות:**
1. להריץ את ה-backend מקומית, ולבצע קריאת `POST /recommendations/cash-allocation` אמיתית (דרך `/docs` של FastAPI, או `curl`/Postman) עם משתמש מאומת אמיתי.
2. תרחיש א': **בלי** `portfolio_id` - לוודא שההמלצה חוזרת עם 6 קטגוריות, סכום האחוזים = 100%, והסכומים בש"ח תואמים חישובית לסכום שהוזן.
3. תרחיש ב': **עם** `portfolio_id` קיים שיש בו אחזקות אמיתיות - לוודא (ידנית, בקריאת הטקסט/הנימוק שחוזר מה-AI) שההמלצה בפועל "מודעת" לריכוזיות הקיימת בתיק (למשל לא ממליצה על עוד ריכוז באותו נכס שכבר קיים בכמות גדולה).
4. לוודא שה-cooldown (20 שניות) אכן חוסם קריאה שנייה מיידית עם 429 תקין.
5. **בדיקת קבלה:** שני התרחישים עוברים בפועל מול Supabase/Gemini אמיתיים (לא mock), עם תיעוד קצר (למשל screenshot של התשובה) שנשמר להוכחה.

### פריט 20 - אימות תוכן השפעת פרופיל המשקיע

**מטרה:** לוודא שההמלצות בפועל **משתנות** בהתאם לפרופיל שונה, לא רק שהפרומפט נכון נשלח (מה שכבר אומת בסעיף 11.1 ב-HANDOVER).

**הוראות:**
1. להריץ ניתוח על **אותו תיק בדיוק** (אותן אחזקות) פעמיים: פעם אחת עם פרופיל "שמרן, גיל 60, טווח 3 שנים", ופעם שנייה עם פרופיל "אגרסיבי, גיל 25, טווח 30 שנה".
2. להשוות ידנית את שתי התשובות (report_text + target_weights) ולוודא שיש הבדל משמעותי בהמלצה (למשל יותר משקל לאג"ח/מזומן בפרופיל השמרן).
3. אם **אין** הבדל משמעותי - זהו סימן שהבעיה מסעיף 11.1 (הקפאת Task/Agent) לא נפתרה במלואה, או שהפרומפט לא מדגיש מספיק את בלוק הפרופיל - יש לחזור ל-`_build_investor_profile_block()` ב-`engine/ai_analysis.py` ולחזק את הניסוח (למשל להוסיף משפט מפורש כמו "התאם את ההמלצה במיוחד לפרופיל הבא" בתחילת הבלוק).
4. **בדיקת קבלה:** תיעוד כתוב (טבלה פשוטה) של 2-3 זוגות פרופילים שונים על אותו תיק, עם ההבדל הממשי בהמלצה שהתקבל בכל זוג.

### פריט 21 - Caching תשובות LLM לפי חתימת קלט

**מטרה:** לחסוך קריאות חוזרות ל-Gemini כשאותם נתונים בדיוק נשלחים פעמיים ברצף.

**הוראות:**
1. ליצור פונקציית עזר שמייצרת "חתימה" (hash, למשל `hashlib.sha256`) מתוך ה-metrics dict/פרופיל המשקיע שנשלחים ל-`run_crew_analysis` (ולמקבילה ב-`cash_allocation.py`) - חשוב: ה-hash צריך להיות **דטרמיניסטי** (סדר מפתחות קבוע ב-JSON, למשל `json.dumps(data, sort_keys=True)` לפני ה-hash).
2. להשתמש במנגנון cache פשוט: בהתחלה מספיק **in-memory dict** בתוך התהליך (עם TTL, למשל 5 דקות) - בדומה בעיקרון למטמון ה-60 שניות הקיים כבר באימות (`auth.py`, סעיף 8 פריט 4) - **להעתיק את אותו דפוס**, לא להמציא ספרייה חדשה.
3. **חשוב:** ה-cache הזה משפיע רק על קריאות **זהות לחלוטין** (אותו תיק, אותם מדדים, אותו פרופיל) - אם המשתמש שינה משהו (אפילו שדה פרופיל אחד), החתימה תהיה שונה וה-cache לא ייתפס, זו התנהגות רצויה.
4. **בדיקת קבלה:** להריץ ניתוח פעמיים ברצף מיידי (בין שתי הרצות ה-cooldown הרגיל עדיין חל, אז יש לוודא שהבדיקה מכבדת את זה) עם **אותם** נתונים בדיוק, ולוודא (דרך לוג) שהקריאה השנייה **לא** יצאה בפועל ל-Gemini, אלא הוחזרה מה-cache.

---

## 13.7 הרחבות עתידיות (טווח ארוך)

### פריט 22 - אריזה ל-Google Play / App Store

**מטרה:** לארוז את ה-PWA הקיים כאפליקציה בחנויות.

**הוראות:**
1. **תלוי לחלוטין בפריט 1** (Deployment) - אי אפשר לארוז אפליקציה שמצביעה על `localhost`. לא להתחיל בפריט הזה לפני שיש כתובת production אמיתית.
2. **Android (TWA - Trusted Web Activity):** להשתמש בכלי [Bubblewrap](https://github.com/GoogleChromeLabs/bubblewrap) (CLI רשמי של Google) שמקבל URL של ה-PWA (עם `manifest.json` תקין - כבר קיים דרך `vite-plugin-pwa`, ראו סעיף 2ב ב-HANDOVER) ובונה פרויקט Android מוכן ל-build.
3. **iOS:** להשתמש ב-[Capacitor](https://capacitorjs.com/) - עוטף את ה-build הקיים (`dist/` מ-Vite) בקונטיינר native ומייצר פרויקט Xcode.
4. **בדיקת קבלה:** התקנה בפועל של ה-APK/IPA שנוצר על מכשיר אמיתי (או אמולטור), עם בדיקה שהאפליקציה נטענת ומתחברת ל-backend ב-production בהצלחה.

### פריט 23 - תמיכה ברב-לשוניות (i18n)

**מטרה:** להוסיף אפשרות שפה נוספת (למשל אנגלית) מעבר לעברית הקיימת.

**הוראות:**
1. להתקין ספריית i18n סטנדרטית ל-React (למשל `react-i18next`).
2. לחלץ את **כל** המחרוזות הקבועות בעברית מתוך `Dashboard.jsx`, `PortfolioDetail.jsx`, `CashRecommendation.jsx`, `Login.jsx`, ו-`App.jsx` (Navbar) לקובצי תרגום נפרדים: `he.json` ו-`en.json`.
3. **חשוב - RTL/LTR דינמי:** `index.html` כרגע קבוע ל-`dir="rtl" lang="he"` (סעיף 2ב ב-HANDOVER) - יש להפוך את זה לדינמי (למשל `document.documentElement.dir` משתנה לפי השפה הנבחרת), ולוודא שכל הסגנונות ב-`styles.css` שמשתמשים ב-`border-inline-start` (ולא `border-left`, ראו סעיף 6 פריט 11) ימשיכו לעבוד נכון בשני הכיוונים - זה בדיוק **הסיבה** שנבחרו logical properties מלכתחילה, אז זה אמור לעבוד "בחינם" ברוב המקרים.
4. **הערה חשובה:** תוכן שנוצר ע"י ה-AI (`report_text`) נכתב היום בעברית ע"י Gemini - הוספת שפה תדרוש גם להעביר את שפת הפלט הרצויה כפרמטר לפרומפט של `run_crew_analysis`, לא רק תרגום ממשק המשתמש הסטטי.
5. **בדיקת קבלה:** מעבר שפה בזמן אמת (toggle) גורם לכל הטקסטים הסטטיים בממשק (לא ה-AI output) להתחלף, כולל כיוון RTL/LTR, ללא שבירת פריסה.

### פריט 24 - ייצוא דוחות/היסטוריה ל-Excel/CSV

**מטרה:** אפשרות הורדה בפורמט טבלאי, בנוסף ל-PDF הקיים.

**הוראות:**
1. **Backend:** להוסיף endpoint חדש (למשל `GET /portfolios/{id}/analysis/{aid}/export?format=csv`) ב-`routers/analysis.py` שמייצא את ה-`summary_df` (שכבר מחושב ב-`engine/metrics.py`) לפורמט CSV (`pandas.DataFrame.to_csv`) או Excel (`pandas.DataFrame.to_excel`, דורש `openpyxl`).
2. **לא** ליצור מנוע ייצוא חדש מאפס - `summary_df` כבר קיים בזיכרון בזמן ריצת הניתוח (ראו זרימת הנתונים בסעיף 3א ב-HANDOVER) - רק להוסיף שלב "ייצוא" בסוף התהליך הקיים, או endpoint נפרד שמחשב מחדש מה-DB לפי `analysis_id`.
3. **Frontend:** להוסיף כפתור "הורד כ-Excel/CSV" ליד כפתור "הורד PDF" הקיים ב-`PortfolioDetail.jsx` (אותו דפוס - `window.open` על signed URL, או הורדה ישירה של ה-response כ-blob).
4. **בדיקת קבלה:** הקובץ שיורד נפתח בהצלחה ב-Excel/Google Sheets, ומכיל את אותם הנתונים המספריים כמו הטבלה שמוצגת במסך.

### פריט 25 - תכנון סקיילינג מעבר ל-~100 משתמשים

**מטרה:** זהו פריט **תכנוני/מחקרי**, לא קוד - לבדוק מראש איפה המערכת "תישבר" אם מספר המשתמשים יגדל משמעותית.

**הוראות (לא-טכניות, מחקר בלבד):**
1. לבדוק את מגבלות ה-free tier בפועל של Supabase (מספר שורות/גודל DB/bandwidth), Render (RAM/CPU/שעות שינה של free tier), Vercel (bandwidth), ו-Google Gemini (מכסת בקשות ליום/דקה) - לתעד את המספרים המדויקים כפי שהם **כרגע** (יכולים להשתנות).
2. להצליב מול העומס הצפוי: אם כל משתמש מריץ בממוצע X ניתוחים ביום, בכמה משתמשים תיגמר מכסת Gemini היומית?
3. לתעד ממצאים בטבלה פשוטה: "משאב | מגבלה נוכחית | צפי מתי ניגמר | פעולה מומלצת" - **בלי לממש שום שינוי קוד** בשלב הזה, רק לספק למשתמש תמונת מצב לקבלת החלטה מודעת.
4. **בדיקת קבלה:** מסמך/טבלה קצרה שמוצגת למשתמש, לא קוד.

---

## הערת סיום למודל המבצע

בכל פריט שמומש בפועל - **יש לעדכן את קובץ ה-HANDOVER** (לא רק לכתוב קוד ולסיים): להוסיף שורה/טבלה תחת סעיף 13 המתאים שמסמנת ✅ מומש, בדיוק כפי שנעשה בעבר עבור סעיפים 8, 9, ו-10 (ראו את הדפוס הקיים של טבלאות "פריט | מה ולמה | סטטוס" ב-HANDOVER). **אין לכתוב קוד בלי לתעד את זה בהמשך ב-HANDOVER**, אחרת ה-handover מאבד את התוקף שלו כ"מקור אמת" לפרויקט.
